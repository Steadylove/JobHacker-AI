# AI Provider Configuration
# 支持的提供商: deepseek, openai, groq, together, custom
AI_PROVIDER=deepseek

# AI API Key (根据选择的提供商设置)
# 如果使用 DeepSeek，设置 DEEPSEEK_API_KEY 或 AI_API_KEY
# 如果使用 OpenAI，设置 OPENAI_API_KEY 或 AI_API_KEY
# 如果使用其他服务，设置 AI_API_KEY
AI_API_KEY=your_ai_api_key_here

# 兼容旧配置（可选，如果设置了 AI_API_KEY 则不需要）
# DEEPSEEK_API_KEY=your_deepseek_api_key_here
# OPENAI_API_KEY=your_openai_api_key_here

# 自定义 AI 配置（仅当 AI_PROVIDER=custom 时需要）
# AI_BASE_URL=https://api.custom-ai.com/v1
# AI_MODEL=your-custom-model

# AI 模型和参数（可选，使用默认值）
# AI_MODEL=deepseek-chat  # 或 gpt-4o-mini, llama-3.1-70b-versatile 等
# AI_TEMPERATURE=0.7

# Optional: Cron schedule (default: runs every 6 hours)
# CRON_SCHEDULE=0 */6 * * *
